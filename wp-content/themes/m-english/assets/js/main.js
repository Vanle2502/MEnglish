document.addEventListener('DOMContentLoaded',()=>{const toggle=document.querySelector('.site-header__toggle');const nav=document.querySelector('.site-header__nav');const dropdown=document.querySelector('.site-header__dropdown');const dropButton=dropdown?.querySelector('button');toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(open));toggle.setAttribute('aria-label',open?'Đóng menu':'Mở menu');nav?.classList.toggle('is-open',open)});dropButton?.addEventListener('click',()=>{const open=dropButton.getAttribute('aria-expanded')!=='true';dropButton.setAttribute('aria-expanded',String(open));dropdown.classList.toggle('is-open',open)});document.addEventListener('keydown',event=>{if(event.key==='Escape'){nav?.classList.remove('is-open');dropdown?.classList.remove('is-open');toggle?.setAttribute('aria-expanded','false');dropButton?.setAttribute('aria-expanded','false')}})});
